language: python
sudo: required
before_install: pip install httprunner
script: hrun testcases/test_home_login.yml
after_script:
  - cd /home/travis/build/Nooooooo1/apitest/
  - tree reports
  - git add .
  - git commit -m "[ci skip]  add report"
  - git push -f "https://${git_token}@${git_addr}" master:master
  - cd home
  - git clone https://github.com/Nooooooo1/apitest.git
notifications:
  email:
    recipients:
      - m18392283343@163.com
    on_success: always
