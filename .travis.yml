language: python

sudo: required

before_install: pip install httprunner

install: npm install tree

script: hrun testcases/test_home_login.yml

after_script:
  - cd /home.travis/build/Nooooooo1/
  - git init
  - cd /home/travis/build/Nooooooo1/apitest/
  - git config user.name "No."
  - git config user.email "m18392283343@163.com"
  - git add .
  - git commit -m "[ci skip]  add report"
  - git push --force --quiet  "https://${git_token}@${git_addr}" master:master

notifications:
  email:
    recipients:
      - m18392283343@163.com
    on_success: always
